from .constants import (
    DEFAULT_ENGINE,
    DEFAULT_TIMEOUT,
    DEFAULT_IMPERSONATE,
)


class StealthConfig:
    """Global configuration defaults."""

    DEFAULT_ENGINE = DEFAULT_ENGINE
    DEFAULT_TIMEOUT = DEFAULT_TIMEOUT
    DEFAULT_IMPERSONATE = DEFAULT_IMPERSONATE
